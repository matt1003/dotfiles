#!/bin/bash

if wmctrl -l | grep -q '/bin/bash'; then
  wmctrl -a '/bin/bash'
else
  terminator -l tmux
  while true; do
    if wmctrl -l | grep -q '/bin/bash'; then
      break;
    fi
  done
  wmctrl -r '/bin/bash' -b add,below
fi

if [[ $1 != "--no-kill" ]]; then
  kill -9 $PPID
fi

